import React from 'react';
import ReactDOM from 'react-dom/client';
import CSSLearningApp from './App';
import './tailwind.css';  // Import Tailwind CSS file

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CSSLearningApp />
  </React.StrictMode>
);
